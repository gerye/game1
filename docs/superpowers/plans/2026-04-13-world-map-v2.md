# 江湖世界地图 v2 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 重构世界地图的视觉表现与交互：地图置顶、门派色主导的 Voronoi 领土、3/7 格城池建筑美术、角色图标显示、战斗联动、中国24节气时节命名。

**Architecture:** 所有视觉改动集中在 `src/world-ui.js`；节气命名在 `src/world-tick.js`；地图置顶与战斗蒙层在 `index.html` + `styles.css`；真实战斗触发与角色数据传递在 `src/app.js`。

**Tech Stack:** Canvas 2D API, ES6 modules, CSS position/overlay。

---

## 文件变更地图

| 文件 | 改动 |
|------|------|
| `src/world-tick.js` | 新增 `getSeasonLabel()` 导出；`advanceSeason` 日志使用节气 |
| `src/world-ui.js` | 重写 `renderWorldMap`（接收 entries 参数）；新增 `buildTerritoryMap`；重写 `drawHex`（Voronoi+门派主导）；重写城池绘制为 3格/7格建筑美术；新增角色图标渲染 |
| `src/app.js` | `renderWorldMap` 调用传入 `getEntries()`；触发江湖争霸按钮改为调用 `startChaosBattle()`；触发武道会改为调用 `startTournamentFlow()`；战斗开始/结束时更新蒙层 |
| `index.html` | 将 `section.world-panel-section` 移到 `<main>` 第一个子元素；添加 `#world-battle-overlay` |
| `styles.css` | 添加 `#world-battle-overlay` 样式 |

---

## Task 1：24节气时节命名

**Files:**
- Modify: `src/world-tick.js`

- [ ] **Step 1：在 `src/world-tick.js` 顶部（import 之后）添加节气数据和导出函数**

在文件第一个 `export function` 之前（现有注释行 `// ── 世界状态结构说明` 之前）添加：

```javascript
// ── 24节气 ────────────────────────────────────

// 从小雪开始（游戏开篇为隆冬），每年24节气循环
const SOLAR_TERMS = [
  "小雪", "大雪", "冬至", "小寒", "大寒",
  "立春", "雨水", "惊蛰", "春分", "清明", "谷雨",
  "立夏", "小满", "芒种", "夏至", "小暑", "大暑",
  "立秋", "处暑", "白露", "秋分", "寒露", "霜降", "立冬",
];

const YEAR_LABELS = ["一", "二", "三", "四", "五", "六", "七", "八", "九", "十",
  "十一", "十二", "十三", "十四", "十五", "十六", "十七", "十八", "十九", "二十"];

/**
 * 将数字时节（1 开始）转换为节气标签
 * season 1  → "第一年·小雪"
 * season 24 → "第一年·立冬"
 * season 25 → "第二年·小雪"
 */
export function getSeasonLabel(season) {
  const s = Math.max(1, season);
  const yearIndex = Math.ceil(s / 24) - 1;
  const termIndex = (s - 1) % 24;
  const yearLabel = yearIndex < YEAR_LABELS.length ? YEAR_LABELS[yearIndex] : `${yearIndex + 1}`;
  return `第${yearLabel}年·${SOLAR_TERMS[termIndex]}`;
}
```

- [ ] **Step 2：修改 `advanceSeason` 中的日志使用节气标签**

找到 `advanceSeason` 函数末尾的这一行：
```javascript
  ws = addWorldLog(ws, `第 ${ws.season} 时节：各派积蓄力量。`);
```
改为：
```javascript
  ws = addWorldLog(ws, `${getSeasonLabel(ws.season)}：各派积蓄力量。`);
```

- [ ] **Step 3：修改 `createWorldState` 中初始日志**

找到：
```javascript
    log: ["江湖初开，六派鼎立。"],
```
改为：
```javascript
    log: [`${getSeasonLabel(1)}，江湖初开，六派鼎立。`],
```

- [ ] **Step 4：提交**

```bash
git add src/world-tick.js
git commit -m "feat: 24节气时节命名（小雪起始，每年循环）"
```

---

## Task 2：地图置顶 + 战斗蒙层

**Files:**
- Modify: `index.html`
- Modify: `styles.css`
- Modify: `src/app.js`

- [ ] **Step 1：将世界地图 section 移到 `<main>` 最前面**

读取 `index.html`，找到：
```html
      <section class="panel world-panel-section" aria-labelledby="world-heading">
```
这一整个 section（从 `<section class="panel world-panel-section"` 到对应 `</section>`）。

将它**剪切**并**粘贴**到 `<main class="layout" id="main-content">` 标签之后、原第一个 `<section class="panel upload-panel"` 之前。

同时把其他三个 section 的标题序号更新：
- `<h2 id="upload-heading">1. 识别与建档</h2>` → `<h2 id="upload-heading">2. 识别与建档</h2>`
- `<h2 id="codex-heading">` 里若有序号 `2.` → `3.`
- `<h2 id="battle-heading">3. 自动战场</h2>` → `<h2 id="battle-heading">4. 自动战场</h2>`
- 世界地图的 `<h2 id="world-heading">4. 江湖世界地图</h2>` → `<h2 id="world-heading">1. 江湖世界地图</h2>`

- [ ] **Step 2：在世界地图 Canvas 外层添加 `#world-battle-overlay`**

在 world-panel-section 中，找到：
```html
        <div class="world-map-layout">
          <canvas id="world-map-canvas" ...></canvas>
```
将 canvas 包裹在相对定位容器中，并添加蒙层：

```html
        <div class="world-map-layout">
          <div class="world-canvas-wrap" style="position:relative; display:inline-block;">
            <canvas id="world-map-canvas" width="860" height="680" style="border:1px solid #444; background:#1a1a2e; display:block;"></canvas>
            <div id="world-battle-overlay" class="world-battle-overlay" hidden>
              <div class="world-battle-overlay-text">⚔ 战斗进行中<br><small>请前往战场区域观看</small></div>
            </div>
          </div>
          <div id="world-arbiter-panel" style="min-width:280px; padding:12px;"></div>
```

关闭 `</div>` 对应 `world-map-layout` 保持不变。

- [ ] **Step 3：在 `styles.css` 末尾追加蒙层样式**

```css
/* ── 世界地图战斗蒙层 ───────────────────────── */
.world-battle-overlay {
  position: absolute;
  top: 0; left: 0;
  width: 100%; height: 100%;
  background: rgba(0, 0, 0, 0.62);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  z-index: 10;
  border: 2px solid rgba(255, 180, 50, 0.5);
}

.world-battle-overlay-text {
  color: #f5c842;
  font-size: 20px;
  font-weight: bold;
  text-align: center;
  line-height: 1.6;
  text-shadow: 0 2px 8px rgba(0,0,0,0.8);
}

.world-battle-overlay-text small {
  font-size: 13px;
  color: #ccc;
  font-weight: normal;
}
```

- [ ] **Step 4：在 `src/app.js` 的 `dom` 对象中添加蒙层引用**

找到 `dom` 对象中 `worldArbiterPanel` 的那行，在其后添加：
```javascript
  worldBattleOverlay: document.getElementById("world-battle-overlay"),
```

- [ ] **Step 5：在 `src/app.js` 中添加蒙层显示/隐藏函数**

在 `bindEvents()` 函数之前（约第184行附近），添加：

```javascript
function setWorldBattleOverlay(visible) {
  if (!dom.worldBattleOverlay) return;
  dom.worldBattleOverlay.hidden = !visible;
}
```

- [ ] **Step 6：战斗开始时显示蒙层，战斗结束时隐藏**

在 `async function startChaosBattle()` 函数体首行（约第1752行，`state.activeBattleMode = "chaos";` 之后）添加：
```javascript
  setWorldBattleOverlay(true);
```

找到 `applyBattleRewards` 函数末尾（`} finally {` 块里 `state.rewardProcessing = false;` 之后），添加：
```javascript
    setWorldBattleOverlay(false);
    renderWorldMap(dom.worldMapCanvas, state.worldState, state.worldViewState);
    renderArbiterPanel(dom.worldArbiterPanel, state.worldState);
```

同样在 `startTournamentFlow` 开始时添加 `setWorldBattleOverlay(true);`，`finishTournament` 末尾添加 `setWorldBattleOverlay(false);`。

对 `startRankingFlow` 和 `finishRanking` 同样处理。

- [ ] **Step 7：点击蒙层时滚动到战场**

在 `bindEvents()` 末尾，添加：
```javascript
  dom.worldBattleOverlay?.addEventListener("click", () => {
    document.getElementById("battle-heading")?.scrollIntoView({ behavior: "smooth", block: "start" });
  });
```

- [ ] **Step 8：提交**

```bash
git add index.html styles.css src/app.js
git commit -m "feat: 世界地图置顶，战斗进行时显示蒙层"
```

---

## Task 3：Voronoi 领土着色（门派色主导）

**Files:**
- Modify: `src/world-ui.js`

- [ ] **Step 1：在 `src/world-ui.js` 中添加 `buildTerritoryMap` 函数**

在 `drawHex` 函数之前（`// ── Canvas 渲染` 注释块内），添加：

```javascript
/**
 * 预计算 Voronoi 领土：每个格子归属最近的有主城池
 * 返回 Map<"q,r", factionId>
 * @param {Object[]} cityStates  worldState.cities
 */
function buildTerritoryMap(cityStates) {
  const activeCities = cityStates
    .filter((c) => c.faction)
    .map((c) => {
      const template = ALL_CITIES.find((t) => t.id === c.id);
      return template ? { q: template.q, r: template.r, faction: c.faction } : null;
    })
    .filter(Boolean);

  const map = new Map();
  if (!activeCities.length) return map;

  for (let q = -WORLD_MAP_RADIUS; q <= WORLD_MAP_RADIUS; q++) {
    for (let r = -WORLD_MAP_RADIUS; r <= WORLD_MAP_RADIUS; r++) {
      if (!inBounds(q, r)) continue;
      let nearestFaction = null;
      let nearestDist = Infinity;
      for (const city of activeCities) {
        const d = hexDistance(q, r, city.q, city.r);
        if (d < nearestDist) { nearestDist = d; nearestFaction = city.faction; }
      }
      if (nearestFaction) map.set(`${q},${r}`, nearestFaction);
    }
  }
  return map;
}
```

- [ ] **Step 2：重写 `drawHex` 使用 Voronoi 地图，门派色 90%**

将现有 `drawHex(ctx, q, r, cityOwnership)` 函数**整体替换**为：

```javascript
function drawHex(ctx, q, r, territoryMap) {
  const { x, y } = hexToPixel(q, r);
  const corners = hexCorners(x, y, HEX_SIZE - 0.5);

  ctx.beginPath();
  ctx.moveTo(corners[0].x, corners[0].y);
  for (let i = 1; i < 6; i++) ctx.lineTo(corners[i].x, corners[i].y);
  ctx.closePath();

  const terrain = getTerrainAt(q, r);
  const terrainColor = TERRAIN_COLORS[terrain] || "#cccccc";
  const faction = territoryMap?.get(`${q},${r}`);

  if (faction) {
    // 门派色主导（实色填充），极细地形边框作为纹理提示
    ctx.fillStyle = FACTION_COLORS[faction];
    ctx.fill();
    ctx.strokeStyle = terrainColor + "44";
    ctx.lineWidth = 0.6;
    ctx.stroke();
  } else {
    // 中立格：地形色
    ctx.fillStyle = terrainColor;
    ctx.fill();
    ctx.strokeStyle = "#33333344";
    ctx.lineWidth = 0.3;
    ctx.stroke();
  }
}
```

- [ ] **Step 3：在 `renderWorldMap` 中预计算 Voronoi 并传入 `drawHex`**

将 `renderWorldMap` 内原有的代码从：
```javascript
  const cityOwnership = Object.fromEntries(
    (worldState.cities || []).map((c) => [c.id, c.faction])
  );

  // 绘制所有格子
  for (let q = -WORLD_MAP_RADIUS; q <= WORLD_MAP_RADIUS; q++) {
    for (let r = -WORLD_MAP_RADIUS; r <= WORLD_MAP_RADIUS; r++) {
      if (!inBounds(q, r)) continue;
      drawHex(ctx, q, r, cityOwnership);
    }
  }
```
改为：
```javascript
  const territoryMap = buildTerritoryMap(worldState.cities || []);
  const cityOwnership = Object.fromEntries(
    (worldState.cities || []).map((c) => [c.id, c.faction])
  );

  // 绘制所有格子
  for (let q = -WORLD_MAP_RADIUS; q <= WORLD_MAP_RADIUS; q++) {
    for (let r = -WORLD_MAP_RADIUS; r <= WORLD_MAP_RADIUS; r++) {
      if (!inBounds(q, r)) continue;
      drawHex(ctx, q, r, territoryMap);
    }
  }
```

- [ ] **Step 4：提交**

```bash
git add src/world-ui.js
git commit -m "feat: Voronoi 领土着色，门派色主导"
```

---

## Task 4：城池建筑美术（3格/7格图标）

**Files:**
- Modify: `src/world-ui.js`

本任务将城池标记从简单图形（星/菱形/圆点）升级为多格建筑美术：
- **小城（SMALL）**：占 3 格（中心格 + 左右各1格），绘制"驿站"图标
- **大城（LARGE）**：占 7 格（中心格 + 6邻格），绘制"城池"图标
- **总部（HQ）**：占 7 格，绘制"宫殿/要塞"图标

城池格子使用**更深的门派色**填充，与周围领土区分；图标使用白色/浅色绘制，轮廓清晰。

- [ ] **Step 1：在 `src/world-ui.js` 中添加城池格子深色填充辅助函数**

在 `drawHex` 函数之后添加：

```javascript
// 辅助：计算某颜色的暗化版本（简单混合黑色）
function darkenColor(hexColor, amount = 0.4) {
  const r = parseInt(hexColor.slice(1, 3), 16);
  const g = parseInt(hexColor.slice(3, 5), 16);
  const b = parseInt(hexColor.slice(5, 7), 16);
  const dr = Math.round(r * (1 - amount));
  const dg = Math.round(g * (1 - amount));
  const db = Math.round(b * (1 - amount));
  return `rgb(${dr},${dg},${db})`;
}

// 填充指定 (q,r) 格子为深色（城池格子使用）
function fillHexDark(ctx, q, r, color) {
  const { x, y } = hexToPixel(q, r);
  const corners = hexCorners(x, y, HEX_SIZE - 0.5);
  ctx.beginPath();
  ctx.moveTo(corners[0].x, corners[0].y);
  for (let i = 1; i < 6; i++) ctx.lineTo(corners[i].x, corners[i].y);
  ctx.closePath();
  ctx.fillStyle = color;
  ctx.fill();
  ctx.strokeStyle = "rgba(0,0,0,0.4)";
  ctx.lineWidth = 0.8;
  ctx.stroke();
}

// 六邻格坐标（axial 坐标系）
const HEX_NEIGHBOR_DIRS = [
  [1, 0], [1, -1], [0, -1], [-1, 0], [-1, 1], [0, 1]
];
```

- [ ] **Step 2：添加小城驿站图标绘制函数**

```javascript
/**
 * 小城：3格（中心 + 东 + 西）填充暗色，绘制驿站图标
 * 图标：两个小塔楼夹一个城门洞
 */
function drawSmallCityBuilding(ctx, city, factionColor) {
  const dark = darkenColor(factionColor, 0.35);
  const { q, r } = city;

  // 填充3格（中心、右邻、左邻）
  fillHexDark(ctx, q, r, dark);
  fillHexDark(ctx, q + 1, r, dark);
  fillHexDark(ctx, q - 1, r, dark);

  // 绘制图标（以中心像素为原点）
  const { x, y } = hexToPixel(q, r);
  const ic = "rgba(255,255,255,0.92)";

  ctx.fillStyle = ic;

  // 左塔
  ctx.fillRect(x - 9, y - 6, 5, 9);
  // 左塔垛口
  ctx.fillRect(x - 10, y - 8, 2, 3);
  ctx.fillRect(x - 7, y - 8, 2, 3);

  // 右塔
  ctx.fillRect(x + 4, y - 6, 5, 9);
  // 右塔垛口
  ctx.fillRect(x + 3, y - 8, 2, 3);
  ctx.fillRect(x + 6, y - 8, 2, 3);

  // 中间城墙（连接两塔）
  ctx.fillRect(x - 3, y - 3, 6, 6);

  // 城门拱洞
  ctx.fillStyle = dark;
  ctx.beginPath();
  ctx.arc(x, y + 1, 2, Math.PI, 0);
  ctx.rect(x - 2, y + 1, 4, 3);
  ctx.fill();
}
```

- [ ] **Step 3：添加大城图标绘制函数**

```javascript
/**
 * 大城：7格（中心+6邻格）填充暗色，绘制城池图标
 * 图标：六边形城墙轮廓 + 内部三角屋顶建筑
 */
function drawLargeCityBuilding(ctx, city, factionColor) {
  const dark = darkenColor(factionColor, 0.3);
  const { q, r } = city;

  // 填充7格
  fillHexDark(ctx, q, r, dark);
  for (const [dq, dr] of HEX_NEIGHBOR_DIRS) {
    fillHexDark(ctx, q + dq, r + dr, dark);
  }

  // 绘制图标
  const { x, y } = hexToPixel(q, r);
  const ic = "rgba(255,255,255,0.90)";
  ctx.strokeStyle = ic;
  ctx.lineWidth = 1.2;

  // 城墙六边形轮廓（稍小于格子）
  const wallR = HEX_SIZE * 1.6;
  ctx.beginPath();
  for (let i = 0; i < 6; i++) {
    const angle = (Math.PI / 3) * i - Math.PI / 6;
    const wx = x + wallR * Math.cos(angle);
    const wy = y + wallR * Math.sin(angle);
    if (i === 0) ctx.moveTo(wx, wy); else ctx.lineTo(wx, wy);
  }
  ctx.closePath();
  ctx.stroke();

  // 城墙四角哨楼（小方块）
  ctx.fillStyle = ic;
  for (let i = 0; i < 6; i += 2) {
    const angle = (Math.PI / 3) * i - Math.PI / 6;
    const wx = x + wallR * Math.cos(angle);
    const wy = y + wallR * Math.sin(angle);
    ctx.fillRect(wx - 1.5, wy - 1.5, 3, 3);
  }

  // 内部主建筑（宝塔形：三角+矩形叠层）
  ctx.fillStyle = ic;
  // 底层
  ctx.fillRect(x - 5, y + 1, 10, 5);
  // 中层
  ctx.fillRect(x - 3, y - 3, 6, 5);
  // 屋顶
  ctx.beginPath();
  ctx.moveTo(x - 5, y - 3);
  ctx.lineTo(x, y - 8);
  ctx.lineTo(x + 5, y - 3);
  ctx.closePath();
  ctx.fill();
}
```

- [ ] **Step 4：添加总部图标绘制函数**

```javascript
/**
 * 总部：7格填充，绘制宫殿图标
 * 图标：双层城墙 + 中央高塔（区别于大城的高度和装饰）
 */
function drawHQBuilding(ctx, city, factionColor) {
  const dark = darkenColor(factionColor, 0.25);
  const { q, r } = city;

  // 填充7格（更亮一点，HQ比大城显眼）
  const darkHQ = darkenColor(factionColor, 0.2);
  fillHexDark(ctx, q, r, darkHQ);
  for (const [dq, dr] of HEX_NEIGHBOR_DIRS) {
    fillHexDark(ctx, q + dq, r + dr, dark);
  }

  const { x, y } = hexToPixel(q, r);
  const ic = "rgba(255,245,200,0.95)";  // 金白色，区别于大城的纯白
  ctx.strokeStyle = ic;

  // 外城墙
  ctx.lineWidth = 1.4;
  const outerR = HEX_SIZE * 1.7;
  ctx.beginPath();
  for (let i = 0; i < 6; i++) {
    const angle = (Math.PI / 3) * i - Math.PI / 6;
    const wx = x + outerR * Math.cos(angle);
    const wy = y + outerR * Math.sin(angle);
    if (i === 0) ctx.moveTo(wx, wy); else ctx.lineTo(wx, wy);
  }
  ctx.closePath();
  ctx.stroke();

  // 内城墙（小一圈）
  ctx.lineWidth = 0.8;
  const innerR = HEX_SIZE * 1.0;
  ctx.beginPath();
  for (let i = 0; i < 6; i++) {
    const angle = (Math.PI / 3) * i - Math.PI / 6;
    const wx = x + innerR * Math.cos(angle);
    const wy = y + innerR * Math.sin(angle);
    if (i === 0) ctx.moveTo(wx, wy); else ctx.lineTo(wx, wy);
  }
  ctx.closePath();
  ctx.stroke();

  // 六角全填充（城楼）
  ctx.fillStyle = ic;
  for (let i = 0; i < 6; i++) {
    const angle = (Math.PI / 3) * i - Math.PI / 6;
    const wx = x + outerR * Math.cos(angle);
    const wy = y + outerR * Math.sin(angle);
    ctx.beginPath();
    ctx.arc(wx, wy, 2, 0, Math.PI * 2);
    ctx.fill();
  }

  // 中央高塔（比大城高出约1.5倍）
  ctx.fillStyle = ic;
  // 底座
  ctx.fillRect(x - 4, y + 2, 8, 4);
  // 中层
  ctx.fillRect(x - 3, y - 2, 6, 5);
  // 高层
  ctx.fillRect(x - 2, y - 6, 4, 5);
  // 尖顶
  ctx.beginPath();
  ctx.moveTo(x - 3, y - 6);
  ctx.lineTo(x, y - 11);
  ctx.lineTo(x + 3, y - 6);
  ctx.closePath();
  ctx.fill();
  // 顶部旗帜（小三角形）
  ctx.beginPath();
  ctx.moveTo(x, y - 11);
  ctx.lineTo(x + 4, y - 9);
  ctx.lineTo(x, y - 7);
  ctx.closePath();
  ctx.fillStyle = factionColor;
  ctx.fill();
}
```

- [ ] **Step 5：替换 `renderWorldMap` 中城池标记绘制逻辑**

找到 `renderWorldMap` 中：
```javascript
  // 绘制城池标记
  ALL_CITIES.forEach((city) => {
    const owner = cityOwnership[city.id] ?? city.faction;
    drawCityMarker(ctx, city, owner);
  });
```
替换为：
```javascript
  // 绘制城池建筑（从大到小，保证小城不被大城覆盖）
  const sortedCities = [...ALL_CITIES].sort((a, b) => {
    const tierOrder = { hq: 0, large: 1, small: 2 };
    return (tierOrder[a.tier] ?? 3) - (tierOrder[b.tier] ?? 3);
  });
  sortedCities.forEach((city) => {
    const owner = cityOwnership[city.id] ?? city.faction;
    const color = owner ? FACTION_COLORS[owner] : "#666666";
    if (city.tier === WORLD_CITY_TIERS.HQ) {
      drawHQBuilding(ctx, city, color);
    } else if (city.tier === WORLD_CITY_TIERS.LARGE) {
      drawLargeCityBuilding(ctx, city, color);
    } else {
      drawSmallCityBuilding(ctx, city, color);
    }
  });
```

- [ ] **Step 6：删除已不需要的旧函数 `drawCityMarker` 和 `drawStar`**

删除 `drawCityMarker` 函数（从 `function drawCityMarker` 到其结束 `}`）。
删除 `drawStar` 函数（从 `function drawStar` 到其结束 `}`）。

- [ ] **Step 7：提交**

```bash
git add src/world-ui.js
git commit -m "feat: 城池建筑美术，小城3格驿站/大城7格城池/总部7格宫殿"
```

---

## Task 5：角色图标显示在地图上

**Files:**
- Modify: `src/world-ui.js`
- Modify: `src/app.js`

角色在地图上显示为小圆形图标（12px），内含头像图片（如有）或派系颜色背景，外圈为派系色边框。
- **驻守角色**：显示在其城池坐标附近（城池中心稍微偏移，同城多角色排列）
- **漫游角色**：显示在其 (q, r) 坐标

- [ ] **Step 1：修改 `renderWorldMap` 签名以接受 entries 参数**

将：
```javascript
export function renderWorldMap(canvas, worldState, viewState = {}) {
```
改为：
```javascript
export function renderWorldMap(canvas, worldState, viewState = {}, entries = []) {
```

在 `ctx.restore()` 之前，添加角色图标渲染调用：
```javascript
  // 绘制角色图标
  if (entries.length) {
    drawCharacterIcons(ctx, worldState, entries);
  }
```

- [ ] **Step 2：添加 `drawCharacterIcons` 函数**

在 `renderWorldMap` 之后添加：

```javascript
// 图片缓存（避免重复创建 Image 对象）
const _avatarImageCache = new Map();

function getAvatarImage(dataUrl) {
  if (!dataUrl) return null;
  if (_avatarImageCache.has(dataUrl)) return _avatarImageCache.get(dataUrl);
  const img = new Image();
  img.src = dataUrl;
  _avatarImageCache.set(dataUrl, img);
  return img;
}

/**
 * 在地图上绘制角色图标
 * @param {CanvasRenderingContext2D} ctx
 * @param {Object} worldState
 * @param {Object[]} entries  getEntries() 返回的角色数组（含 base.avatarDataUrl, build.buildId, build.faction.key）
 */
function drawCharacterIcons(ctx, worldState, entries) {
  const charStates = worldState.characterStates || {};
  const cityTemplates = Object.fromEntries(
    (worldState.cities || []).map((c) => {
      const t = ALL_CITIES.find((a) => a.id === c.id);
      return [c.id, t];
    })
  );

  // 按城池聚合驻守角色（错开偏移）
  const cityGroups = {};

  entries.forEach((entry) => {
    const cs = charStates[entry.build?.buildId];
    if (!cs) return;

    let drawQ, drawR;
    if (cs.state === "garrison" && cs.cityId) {
      const t = cityTemplates[cs.cityId];
      if (!t) return;
      drawQ = t.q; drawR = t.r;
      const key = cs.cityId;
      if (!cityGroups[key]) cityGroups[key] = [];
      cityGroups[key].push({ entry, q: drawQ, r: drawR });
    } else if (cs.q != null && cs.r != null) {
      drawSingleCharIcon(ctx, cs.q, cs.r, 0, 0, entry);
    }
  });

  // 驻守角色错开排列（最多显示8个，超出省略）
  Object.values(cityGroups).forEach((group) => {
    group.slice(0, 8).forEach((item, idx) => {
      const offsetX = (idx % 4 - 1.5) * 5;
      const offsetY = Math.floor(idx / 4) * 6 - 3;
      drawSingleCharIcon(ctx, item.q, item.r, offsetX, offsetY, item.entry);
    });
  });
}

function drawSingleCharIcon(ctx, q, r, offsetX, offsetY, entry) {
  const { x, y } = hexToPixel(q, r);
  const cx = x + offsetX;
  const cy = y + offsetY;
  const radius = 4;
  const factionColor = FACTION_COLORS[entry.build?.faction?.key] || "#888";

  // 外圈（派系色）
  ctx.beginPath();
  ctx.arc(cx, cy, radius + 1, 0, Math.PI * 2);
  ctx.fillStyle = factionColor;
  ctx.fill();

  // 头像（clip 到圆形）
  const avatarUrl = entry.base?.avatarDataUrl;
  const img = avatarUrl ? getAvatarImage(avatarUrl) : null;
  if (img && img.complete && img.naturalWidth > 0) {
    ctx.save();
    ctx.beginPath();
    ctx.arc(cx, cy, radius, 0, Math.PI * 2);
    ctx.clip();
    ctx.drawImage(img, cx - radius, cy - radius, radius * 2, radius * 2);
    ctx.restore();
  } else {
    // 无头像：用派系色深色填充
    ctx.beginPath();
    ctx.arc(cx, cy, radius, 0, Math.PI * 2);
    ctx.fillStyle = factionColor + "cc";
    ctx.fill();
  }
}
```

- [ ] **Step 3：在 `src/app.js` 所有 `renderWorldMap` 调用处传入 `getEntries()`**

用 Grep 找到所有 `renderWorldMap(` 的调用行，共约 6 处（在 init、advanceSeasonBtn、triggerJianghuBtn、triggerTournamentBtn、applyBattleRewards、pan/zoom 鼠标事件中）。

每一处从：
```javascript
renderWorldMap(dom.worldMapCanvas, state.worldState, state.worldViewState);
```
改为：
```javascript
renderWorldMap(dom.worldMapCanvas, state.worldState, state.worldViewState, getEntries());
```

- [ ] **Step 4：提交**

```bash
git add src/world-ui.js src/app.js
git commit -m "feat: 角色图标显示在世界地图上（驻守错开排列，漫游在格子上）"
```

---

## Task 6：触发江湖争霸/武道会 = 真实战斗

**Files:**
- Modify: `src/app.js`

当前"触发江湖争霸"按钮是随机选胜者，不运行真实战斗。改为：
- 触发江湖争霸 → 调用 `startChaosBattle()`（战斗结束后已在 `applyBattleRewards` 中自动发放声望）
- 触发武道会 → 调用 `startTournamentFlow()`（结束后在 `finishTournament` 自动发放声望）
- 删除按钮中的随机逻辑

- [ ] **Step 1：找到 `triggerJianghuBtn` 的 click 事件处理器**

在 `src/app.js` 中找到（约 bindEvents 函数内）：

```javascript
  dom.triggerJianghuBtn?.addEventListener("click", async () => {
    const factionIds = ["qingyun", "shaolin", "mojiao", "jiaoting", "xiandao", "hundian"];
    const winner = factionIds[Math.floor(Math.random() * factionIds.length)];
    state.worldState = applyJianghuPrestige(state.worldState, winner);
    await state.storage.putWorldState(state.worldState);
    renderWorldMap(dom.worldMapCanvas, state.worldState, state.worldViewState, getEntries());
    renderArbiterPanel(dom.worldArbiterPanel, state.worldState);
  });
```

**替换**为：

```javascript
  dom.triggerJianghuBtn?.addEventListener("click", () => {
    // 触发真实江湖争霸战斗；声望在 applyBattleRewards 结束时自动发放
    startChaosBattle().catch((error) => {
      console.error("触发江湖争霸失败：", error);
    });
    // 滚动到战场
    document.getElementById("battle-heading")?.scrollIntoView({ behavior: "smooth", block: "start" });
  });
```

- [ ] **Step 2：找到 `triggerTournamentBtn` 的 click 事件处理器**

找到：
```javascript
  dom.triggerTournamentBtn?.addEventListener("click", async () => {
    const { computePowerScore, FACTION_IDS } = await import("./faction-state.js");
    const factionRanks = [...FACTION_IDS].sort((a, b) =>
      computePowerScore(b, state.worldState.factionStats, state.worldState.cities) -
      computePowerScore(a, state.worldState.factionStats, state.worldState.cities)
    );
    state.worldState = applyRankedEventPrestige(state.worldState, "tournament", factionRanks);
    await state.storage.putWorldState(state.worldState);
    renderWorldMap(dom.worldMapCanvas, state.worldState, state.worldViewState, getEntries());
    renderArbiterPanel(dom.worldArbiterPanel, state.worldState);
  });
```

**替换**为：

```javascript
  dom.triggerTournamentBtn?.addEventListener("click", () => {
    // 触发真实武道会；声望在 finishTournament 结束时自动发放
    startTournamentFlow().catch((error) => {
      console.error("触发武道会失败：", error);
    });
    document.getElementById("battle-heading")?.scrollIntoView({ behavior: "smooth", block: "start" });
  });
```

- [ ] **Step 3：确认 `applyRankedEventPrestige` import 是否仍被使用**

用 Grep 搜索 `applyRankedEventPrestige` 在 `app.js` 中的使用位置。如果除了 `finishTournament` 和 `finishRanking` 之外没有其他使用，则 import 保留（在 `finishTournament`/`finishRanking` 中仍需要）。

同样检查 `applyJianghuPrestige` 是否仍在 `applyBattleRewards` 中使用。如是，import 保留。

- [ ] **Step 4：提交**

```bash
git add src/app.js
git commit -m "feat: 触发江湖争霸/武道会按钮改为启动真实战斗"
```

---

## Task 7：仲裁者面板使用节气标签

**Files:**
- Modify: `src/world-ui.js`

- [ ] **Step 1：在 `src/world-ui.js` 中导入 `getSeasonLabel`**

在文件顶部的 import 语句中，添加：
```javascript
import { getSeasonLabel } from "./world-tick.js";
```

- [ ] **Step 2：修改 `renderArbiterPanel` 使用节气标签**

找到：
```javascript
      <h3>第 ${worldState.season || 1} 时节 · 江湖格局</h3>
```
改为：
```javascript
      <h3>${getSeasonLabel(worldState.season || 1)} · 江湖格局</h3>
```

- [ ] **Step 3：提交**

```bash
git add src/world-ui.js
git commit -m "feat: 仲裁者面板时节标题使用24节气标签"
```

---

## 自检

**规格覆盖：**
- ✅ 地图在最上面 — Task 2 Step 1
- ✅ 战斗时显示蒙层（可点击滚动到战场） — Task 2 Step 2-7
- ✅ 门派色主导（90% + 地形细边框） — Task 3
- ✅ Voronoi 领土划分（整张地图按最近城池分割） — Task 3
- ✅ 小城3格建筑（驿站：两塔+城门） — Task 4
- ✅ 大城7格建筑（城池：六边形城墙+宝塔） — Task 4
- ✅ 总部7格建筑（宫殿：双重城墙+高塔+金旗） — Task 4
- ✅ 角色图标（驻守错开排列/漫游在格上） — Task 5
- ✅ 触发江湖争霸 = 真实战斗 — Task 6
- ✅ 触发武道会 = 真实武道会 — Task 6
- ✅ 24节气命名（小雪起始） — Task 1 + Task 7

**潜在风险：**
- `buildTerritoryMap` 每次渲染都遍历所有格（约5000格 × 48城 = 24万次）。在拖动地图时会每帧调用，可能影响性能。建议缓存：当 `worldState.cities` 引用未变时，复用上一次的 territoryMap。可以在 `renderWorldMap` 外部用 `let _lastCityStates, _cachedTerritoryMap` 缓存。
- 小城的 3 格（center + q+1 + q-1）在地图边缘可能超出地图范围，`fillHexDark` 中对 `inBounds` 的检查需要在实现时加上。
